[package]
name = "downstream-docs-check"
version = "0.0.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
lingxi-llm-client = { path = "../.." }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
